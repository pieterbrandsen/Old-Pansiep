module.exports = {
  run: function() {
    // If Memory.mainSystem Is Defined And Enabled //
    if (Memory.mainSystem)
    if (Memory.mainSystem.cpuTracker == true)
    return true;

    return false;
  }
}
